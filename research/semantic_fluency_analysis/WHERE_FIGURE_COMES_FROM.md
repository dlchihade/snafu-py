# WHERE THE "EXPLOITATION VS. EXPLORATION METRICS" FIGURE COMES FROM

## 📊 Figure Source

**Script:** `create_exploit_explore_metrics_figure.py`

**Output Files:**
- `output/exploit_explore_metrics.png`
- `output/exploit_explore_metrics.pdf`

**Last Generated:** November 30, 2024 (20:07)

---

## 📍 Data Source

**File:** `output/phase_coherence_analysis_all_participants.csv`

**Loaded at:** Line 174-182 of `create_exploit_explore_metrics_figure.py`

```python
data_path = Path('output/phase_coherence_analysis_all_participants.csv')
df = pd.read_csv(data_path)
```

---

## 🔍 Panel C: Inter-Phase Mean (Bottom-Left)

**Code Location:** Lines 235-258

**Data Used:**
- **Exploitation:** `df['inter_phase_mean_exploitation'].values` (Line 240)
- **Exploration:** `df['inter_phase_mean_exploration'].values` (Line 241)

**These values come from:**
- Generated by `phase_coherence_analysis.py`
- Computed at:
  - **Line 305:** `exploitation_inter_mean = np.mean(exploitation_inter_similarities)`
  - **Line 323:** `exploration_inter_mean = np.mean(exploration_inter_similarities)`
- Saved to CSV at: **Line 550** of `phase_coherence_analysis.py`

---

## 📈 What the Figure Shows

The figure displays **boxplots** which show:
- **Median** (line in the middle of the box) - NOT the mean!
- **Interquartile Range (IQR)** (the box)
- **Whiskers** (extend to min/max, excluding outliers)
- **Outliers** (individual points)

**The median values you see (≈0.79 for Exploitation, ≈0.33 for Exploration) are:**
- Computed from the individual participant values in the CSV
- Displayed as the median line in each boxplot
- Different from the group mean values we calculated earlier

---

## 🔗 Complete Data Flow

1. **`phase_coherence_analysis.py`** (Lines 305, 323)
   - Computes `inter_phase_mean_exploitation` and `inter_phase_mean_exploration` for each participant
   - Saves to `output/phase_coherence_analysis_all_participants.csv`

2. **`create_exploit_explore_metrics_figure.py`** (Line 174)
   - Loads the CSV file
   - Extracts the columns (Lines 240-241)
   - Creates boxplots showing the distribution

3. **Figure Output** (Lines 298-307)
   - Saves as PNG and PDF
   - Shows median, IQR, and outliers for each metric

---

## ⚠️ Important Note

The **median** values shown in the figure (≈0.79, ≈0.33) are different from the **mean** values we calculated (0.7051, 0.7589). 

The figure shows the **distribution** of values across participants, with the median as the central tendency measure displayed in the boxplot.






